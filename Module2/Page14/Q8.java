// HW_Stack Syntax learning using Character

// Declare an Empty stack s.
// Take Single integer T as input.
// For next T Lines format (case, x (optional))
// case 1. Print the size of the stack in a separate line.
// case 2. Remove an element from the stack. If the stack is empty then print —1 in a separate line.
// case 3. Add Character x to the stack s.
// case 4. Print a Character at the top of the stack. If the stack is empty print —1 in a separate line.
// Input Format

// Single Integer T.
// T Lines of format (case, x (optional)).
// Constraints

// 1 ≤ T ≤ 10^4
// a ≤ x ≤ z
// Output Format

// According to case Print the information explained, if it's not possible then -1.


